
// This is the javascript for begin.html
// we have removed the "name" completely from html and script." - 

// Generated by CoffeeScript 1.9.3
(function() {
  jQuery(function() {
    return $("body").on('click', 'button', function(event) {
      var whereto;  //tells us where to go 
      if (newstatus === "True") {
        /*if ($("#name").val().length < 2) {
          $("#name").parent().addClass('has-error').addClass('has-feedback');   //throws an error
          $("#name").next().removeClass('hidden');
          return $("#namealert").removeClass('hidden');
        } 
        // if name entered is more than 2 chars
        else {*/
          //whereto = 'task?uniqueId=' + uniqueId + '&experimentName=' + event.target.id + '&debug=' + debug + '&name=' + $("#name").val();
          whereto = 'task?uniqueId=' + uniqueId + '&experimentName=' + event.target.id + '&debug=' + debug;
          return window.location = whereto;
        //}
      }
      // if newStatus == "False" --> 
       else { 
        whereto = 'task?uniqueId=' + uniqueId + '&experimentName=' + event.target.id + '&debug=' + debug;
        return window.location = whereto;
      }
    });
  });

}).call(this);
